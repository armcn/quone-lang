scores <- data.frame(name = c("Alice", "Bob", "Carol"), score = c(85.0, 92.0, 78.0))

names <- scores$name

mean_score <- mean(scores$score)

total <- sum(scores$score)

rounded_mean <- round(mean(scores$score))

n <- length(scores$name)

